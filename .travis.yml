language: python
python:
  - '2.7'
sudo: required
dist: trusty
install:
  - source cfg4dev
  - pip install coveralls
  # install the lastest version of chrome
  - wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
  - sudo dpkg -i google-chrome*.deb
  - google-chrome --version
  # install chromedriver
  - curl -s --output chromedriver.zip http://chromedriver.storage.googleapis.com/2.29/chromedriver_linux64.zip
  - unzip chromedriver.zip
  - sudo mv chromedriver /usr/local/bin/.
  - sudo chown root.root /usr/local/bin/chromedriver
  - sudo chmod a+x /usr/local/bin/chromedriver
  - docker pull koalaman/shellcheck:latest
script:
  - pep8
  - flake8
  - docker run -v "$PWD:/mnt" koalaman/shellcheck:latest $(find . -name \*.sh | grep -v \.env)
  - nosetests --verbose --with-coverage --cover-branches --cover-erase --cover-package cloudfeaster bin/tests cloudfeaster
  # '|| true' because sometimes push to coveralls fails but the build
  # should not fail because of that error
  - coveralls || true
  - python setup.py sdist --formats=gztar
  - if [ "$TRAVIS_BRANCH" == "master" ] || [ "$TRAVIS_TAG" != "" ]; then ./dockerfiles/build-docker-image.sh -v -t "$TRAVIS_TAG" ./dist/cloudfeaster-*.*.*.tar.gz $DOCKER_USERNAME $DOCKER_PASSWORD; fi
